City = class City {
  constructor(name) {
    this.name = name
  }
};
// вообщем подумать насчет горродов, может, у нас на карте не города а дороги
// и вот у тодоги как раз есть 2 города и ток ее координаты(получается не нужно запариваться куда откуда можем пойти)